# Sampling MPC

## Instalacja 

Przed uruchomieniem skryptu `experiment.py` należy zainstalować gymnasium z obsługą środowisk classic_control oraz mujoco:

```bash
pip install gymnasium[classic_control]
pip install gymnasium[mujoco]
```